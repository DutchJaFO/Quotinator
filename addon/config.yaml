name: Quotinator
version: "1.0.2"
slug: quotinator
description: Self-hosted quote REST API with Blazor management UI.
url: https://github.com/DutchJaFO/Quotinator
arch:
  - amd64
  - aarch64

# Pre-built image from GitHub Container Registry.
# The supervisor appends the version tag automatically.
image: ghcr.io/dutchjafo/quotinator

startup: application
boot: auto

# Direct access port — user can override the host-side port in HA if 8080 is taken.
ports:
  8080/tcp: 8080
ports_description:
  8080/tcp: Direct web access (alternative to ingress)

# Ingress — embeds the UI into the Home Assistant sidebar.
# Uses a dedicated internal port (8099) separate from the direct access port (8080).
ingress: true
ingress_port: 8099

# Data directory — quotes.json is stored here and persists across updates.
map:
  - data:rw
