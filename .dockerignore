**/.git
**/.vs
**/bin
**/obj
**/*.user
**/TestResults

# Local developer overrides — must never reach the image
**/appsettings.local.json

# Not needed in the image
tests/
docs/
scripts/
docker/
addon/
schemas/
.github/
